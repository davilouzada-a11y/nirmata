# Copy to .env and adjust. Consumed by docker-compose.

# Backend
SECRET_KEY=change-me-to-a-long-random-value
DEIDENT_SALT=change-me-secret-deident-salt
# mock = no ML deps · xrv = real pretrained model (needs torch+torchxrayvision) · torch = own checkpoint
ML_BACKEND=mock
SEED_ADMIN_EMAIL=radiologista@example.com
SEED_ADMIN_PASSWORD=changeme123

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
